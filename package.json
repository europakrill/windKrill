{
  "name": "windkrill",
  "private": true,
  "version": "0.1.0",
  "description": "Fully open-source pixel-faithful WindTerm reimplementation (Windows-first). Tauri 2 + TypeScript + Rust terminal engine.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^8.2.2"
  }
}
